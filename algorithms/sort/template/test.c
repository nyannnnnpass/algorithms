
int create_array(int **array, int n, int range);

#include "utils.h"

int main(int argc, char *argc[])
{
  int size;
  int *array;

  if (argc != 2) {
    printf("Usage: %s <length_of_array> <range_of_number>\n", argv[0]);
  }

  create_array(&array, 
}
